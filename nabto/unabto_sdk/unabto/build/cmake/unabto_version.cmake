if ($ENV{SVN_REVISION})
  set(SVN_VERSION $ENV{SVN_REVISION})
elseif($ENV{SVN_REVISION_1})
  set(SVN_VERSION $ENV{SVN_REVISION_1})
else()
  set(SVN_VERSION 0)
endif()

if ($ENV{BUILD_NUMBER})
  set(BUILD_NUMBER $ENV{BUILD_NUMBER})
else()
  set(BUILD_NUMBER 5678)
endif()

message("svn revision ${SVN_REVISION}")

set(NABTO_MAJOR_VERSION 2)

if(${SVN_VERSION}) 
  set(NABTO_MINOR_VERSION ${SVN_VERSION})
else()
  set(NABTO_MINOR_VERSION 1234)
endif()



set(CPACK_PACKAGE_VERSION_MAJOR ${NABTO_MAJOR_VERSION})
set(CPACK_PACKAGE_VERSION_MINOR ${NABTO_MINOR_VERSION})
set(CPACK_PACKAGE_VERSION_PATCH ${BUILD_NUMBER})


message("NABTO_MINOR_VERSION ${NABTO_MINOR_VERSION} NABTO_MAJOR_VERSION ${NABTO_MAJOR_VERSION}")
